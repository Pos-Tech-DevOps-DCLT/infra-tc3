# ConfigMap do analytics-service
# Contém variáveis NÃO sensíveis
# As credenciais AWS vêm automaticamente via IRSA (ServiceAccount annotation)
# ATENÇÃO: AWS_SQS_URL, AWS_REGION e AWS_DYNAMODB_TABLE são OBRIGATÓRIOS
#          O serviço faz sys.exit(1) se qualquer um estiver ausente
apiVersion: v1
kind: ConfigMap
metadata:
  name: analytics-service-config
  namespace: analytics-service
  labels:
    app: analytics-service
    version: "1.0"
data:
  PORT: "8005"
  AWS_REGION: "us-east-1"
  # Nome da tabela DynamoDB criada pelo Terraform
  AWS_DYNAMODB_TABLE: "tech-challenge-prod-analytics-events"
  # URL da fila SQS — obter com: terraform output -json connection_strings | jq '.sqs_queue_url'
  AWS_SQS_URL: "https://sqs.us-east-1.amazonaws.com/<AWS_ACCOUNT_ID>/tech-challenge-prod-evaluation-events"
