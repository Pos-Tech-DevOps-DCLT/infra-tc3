apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: analytics-service
  labels:
    app: analytics-service
    version: "1.0"
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8005
